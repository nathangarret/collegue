""" 
def approxE(error):
    prev = 1 # Aproximação 0
    current = 2 # Aproximação 1
    i = 2 # Índice de próxima aproximação

    while current - prev > error:
        # Atualizar valores de prev e current
        # prev vira current
        # current += 1/fatorial(i)
        # prev, current = current, current + 1/factorial(i) = factorial?
        # i += 1

    # return current 
"""